"""Humon — a self-hosted, single-process AI agent with hands inside your LAN.

The LLM API is the brain; the host machine is the body. Users talk to Humon over
chat channels (Slack by default) and it executes local tools under a strict,
centrally-enforced permission model. Lean by design, secure by default.
"""

__version__ = "0.1.0"
